# typescript-library-crm
Typescript library for dynamics crm use antd

# Using
Just build and use library like "window.library.ShowLookupField(id)"
This function insert AutoComplete field by antd into element by id.
